DATABASE_URL="file:./dev.db"
JWT_SECRET="change-this-in-production-use-a-long-random-string"
PORT=3001
CORS_ORIGIN="http://localhost:5173"

# Set when the API is on a different domain than the frontend (e.g. GitHub Pages).
# COOKIE_CROSS_SITE=true
# CORS_ORIGIN="https://ikaros2491.github.io"

# Optional — used only when seeding the Doll administrator account
DOLL_PASSWORD="your-secure-password-here"
